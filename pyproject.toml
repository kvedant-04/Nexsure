[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexsure"
version = "2.0.0"
description = "Nexsure AI Platform"
requires-python = ">=3.11,<3.12"
